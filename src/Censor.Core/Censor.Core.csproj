<Project Sdk="Microsoft.NET.Sdk">
  <!-- These sources are also linked into CensorExtension for its stock single-DLL loader. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
</Project>
